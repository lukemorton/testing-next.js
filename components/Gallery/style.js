import style from 'next/css'

const gallery = style({
  display: 'flex',
  flexFlow: 'row wrap',
  alignItems: 'center',
  justifyContent: 'center',
  paddingTop: '3em',
})

export default { gallery }
