import style from 'next/css'

const header = style({
  display: 'block',
  paddingTop: '4em',
  paddingBottom: '1em',
  textAlign: 'center'
})

const logo = style({
  marginLeft: '-.5em'
})

export default { header, logo }
